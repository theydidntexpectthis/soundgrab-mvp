{
  "version": 2,
  "buildCommand": "cd client && npm install && node vercel-build.js",
  "outputDirectory": "client/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
